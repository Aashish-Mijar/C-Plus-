#include<iostream>
#include<cmath>
using namespace std;
int main(){
	double a=2.4, b=3.9,c;
//	cout<<"Enter two numbers:"<<endl;
//	cin>>a>>b;
	
//	c=max(a,b);
//	c=min(a,b);
//  c=pow(a,b);
//	c=sqrt(b);
//	c=abs(-a);
//	c=ceil(a);
//c=floor(a);
//a=pow(a,2);
//b=pow(b,2);
c=sqrt(pow(a,2)+pow(b,2));
	
	cout<<"Value of c is "<<c<<endl;
	return 0;
	
	
}